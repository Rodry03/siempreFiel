name: dbt_protectora
version: '1.0.0'
config-version: 2

profile: protectora

model-paths: ["models"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_protectora:
    staging:
      +materialized: view
    marts:
      +materialized: table
